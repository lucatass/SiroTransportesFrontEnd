import React from 'react';
import logo from './assets/logo.svg';

const Logo: React.FC = () => {
    return (
        <img src={logo} className="logo" alt="logo" />
    );
}
export default Logo;
